# Development environment variables
# Webpack dev server will proxy Socket.IO requests to the backend
# So we can use relative URLs in development

# Leave empty to use webpack proxy, or set to http://localhost:5000 for direct connection
REACT_APP_API_URL=

# Webpack Dev Server Port
PORT=8080

# Auto-open browser
OPEN_BROWSER=true
