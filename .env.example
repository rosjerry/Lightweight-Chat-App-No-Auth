# Flask Configuration
SECRET_KEY=your-secret-key-here-change-in-production
PORT=5000
DEBUG=False

# Socket.IO Configuration
ASYNC_MODE=eventlet

# CORS Configuration
# Comma-separated list of allowed origins (only used when DEBUG=False)
# Default includes common React dev server ports: 3000, 8080, 5173
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:8080,http://127.0.0.1:5173

# Optional: Set to 'true' for verbose Socket.IO logging
# SOCKETIO_DEBUG=False
